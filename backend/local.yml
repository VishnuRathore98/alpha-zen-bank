services:
  postgres:
    build:
      context: .
      dockerfile: ./docker/local/postgres/Dockerfile
    ports:
      - "5433:5432"
    volumes:
      - alpha_zen_local_db:/var/lib/postgresql/data
    env_file:
      - ./.envs/.env.local
    networks:
      - alpha_zen_local_nw

networks:
  alpha_zen_local_nw:
    external: true

volumes:
  alpha_zen_local_db:
